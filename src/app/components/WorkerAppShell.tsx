/**
 * The WorkerAppShell component itself was removed 2026-08-20 (dead: superseded by LandingScreen
 * rendering the same top chrome inline). This type is still live -- imported by App.tsx and
 * WorkerMobileBottomNav.tsx -- so the file stays.
 */
export type WorkerShellScreen = 'landing' | 'upload' | 'summary' | 'processing' | 'workerSettings';
